{
  "name": "billspay-functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint \"src/**/*.ts\""
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "@billspay/backend": "file:../backend",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "typescript": "^5.4.2"
  }
}
